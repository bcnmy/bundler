// import { ExternalSimulationResponseType } from '../types';

// export interface IExternalSimulation<ExternalSimulationData> {
// simulate(simualtionData: ExternalSimulationData): Promise<ExternalSimulationResponseType>
// }

export interface IExternalSimulation {
}

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.server.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.server.name }}
    {{- include "relayers-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.server.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.server.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.server.name }}
      annotations:
        releaseTime: {{ dateInZone "2006-01-02 15:04:05Z" (now) "UTC"| quote }} 
    spec:
      containers:
      - name: {{ .Values.server.name }}
        image: {{ .Values.server.image }}
        ports:
        - containerPort: {{ .Values.server.port }}
        env:
          - name: REDIS_PASSWORD
            value: ""
          - name: REDIS_USERNAME
            value: ""
        envFrom:
          - configMapRef:
                name: {{ .Values.name }}
          - secretRef:
                name: {{ .Values.name }}
        resources:
          requests:
            memory: {{ .Values.server.resource.requests.memory }}
            cpu: {{ .Values.server.resource.requests.cpu }}
          limits:
            memory: {{ .Values.server.resource.limits.memory }}
            cpu: {{ .Values.server.resource.limits.cpu }}
